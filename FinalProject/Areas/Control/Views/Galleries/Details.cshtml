@model FinalProject.Models.Gallery

@{
    ViewData["Title"] = "Details";
}
    <div class="pl-4">
        <h1>Details</h1>

        <div>
            <h4>Gallery</h4>
            <hr />
            <dl class="row">
            </dl>
        </div>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-success">Edit</a> 
            <a asp-action="Index"class="btn btn-primary">Back to List</a>
        </div>
        </div>
